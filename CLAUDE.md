See [AGENTS.md](AGENTS.md).

Short version: **do not read anything under `workshop-docs/`.** It is the
answer key for a workshop whose entire point is diagnosing the app's bugs
from telemetry and source. Work from `pizza-app/` instead.
